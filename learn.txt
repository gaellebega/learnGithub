currently learning git advanced stay tuned to more magics to come have a blast one!:)

git init: which is used to make a repository when normal folder was created
git status usedto help you know the current branch that you are working with
git config --global init.defaultBranch main this is used to move our thing from the master to main
git checkout -b name of the new branch: we use this to create new branch name;
=> this is used to create the new branch but it also switch directly to it 

git checkout used to switch to the new branch.

when we use 
git branch branch-name: this is only used to create the branch but doesn't switch to it
git brach branch-name
git switch brach-name
git switch -c brach-name
or to sum everything up you can write (git checkout -b brach-name)


WHEN YOU HAVE TURNED OFF YOUR LAP

cd Second-Hand-Marketplace-Platform
git checkout ishami
git pull origin ishami
git add .
git commit -m "work"
git push
Pull before you start, push after you finish

REQUEST THE PR

go on the GitHub
then create the PR
from:ishami
to:frontend
//i finished my work on ishami please review and merge it on frontend

then the code review starts

//approved and with comment Looks good and //REQUEST changes fix the bugs

if changes are required 

git checkeout ishami(the branch am working on)

//fix
git add .
git commit -m "fixing all needed"
git push

git checkeout -f main this means go to the branch main even though there is any un committed commits on there

//SOMEONE TO MERGE
merge pull request
ishami->frontend
then your codes are now part of the fontend branch


//GIT HISTORY
git log is used to bring back all the github repository

git revert hashcode is used t undo the chnages that previously made

git log --oneline it  is used to view every single line of commit previously made

the feature branch is what we call the other branches that are created out of the main branch to meannt hey are the sub braches created over the main branch

//TO MERGE
"first we go back to the main branch"
git switch main
git merge brach-name(feature-branch)
//check the log if you want to
git log --oneline
//delete the existing branch
git branch -d brach-name
//push difference
git push :is used to push to the branch that you are currently working on
git push -u origin main: is used to push to the branch main
if you wanna name a brach and then you named it main the gi will say fatal branch already exist cz you already have
branch main so better name it another name used when it is first time pushing the repo,not sure when tracking is set

-u :set up stream

KEY DIFFERENCE

origin main = “do it now”
-u origin main = “do it now + remember for later”
//PS FOR THE FIRST TIME YOU CAN MAKE
you are also allowed to do something like git push origin any-branch-name and then after that you can make the git push
  
  PUSH SUMMARY
git push -u origin main   # first time
git push                  # every time after


git push origin main Meaning:“Push this branch to GitHub right now, but don’t remember anything”

the link between the local repo and the remote repo is called tracking
with using -u you can after do git push
without -u you can always have to say git push origin main like always deliver to the main

//GIT Pull
git pull means get the latest changes from github and then merge into my current branch